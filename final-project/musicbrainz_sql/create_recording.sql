create table Recording(
   id integer,
   gid varchar(40),
   name varchar(40),
   artist_credit integer,
   length integer,
   comment varchar(0),
   edits_pending boolean,
   last_updated varchar(10),
   video varchar(10)
);
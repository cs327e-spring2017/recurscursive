create table Work(
   id integer,
   gid varchar(40),
   name decimal(52,3),
   type varchar(10),
   comment varchar(0),
   edits_pending boolean,
   last_updated decimal(29,3),
   language varchar(10)
);
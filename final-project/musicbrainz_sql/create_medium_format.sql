create table Medium_Format(
   id integer,
   name varchar(20),
   parent integer,
   child_order boolean,
   year integer,
   has_discids varchar(10),
   description varchar(10),
   gid varchar(40)
);
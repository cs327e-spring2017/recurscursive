create table Medium(
   id integer,
   release integer,
   position boolean,
   format boolean,
   name decimal(32,0),
   edits_pending boolean,
   last_updated decimal(29,3),
   track_count integer
);